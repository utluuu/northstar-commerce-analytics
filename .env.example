DB_SERVER=localhost
DB_NAME=NorthstarCommerce
DB_DRIVER=ODBC Driver 18 for SQL Server
# The loader currently enables TrustServerCertificate for local development.
# Prefer Windows integrated authentication. If disabled, set both credentials.
DB_TRUSTED_CONNECTION=yes
DB_USER=
DB_PASSWORD=
